function M = lapSharp(L, E, epsilon)
% just a place holder for what should eventually be the lapsharp operator from [Macdonald, Ruuth, 2009].  This generalizes diagSplit

  if (nargin < 3)
      % do full diagonal splitting, default to eps = \Delta x
  end
  
  error('not implemented');